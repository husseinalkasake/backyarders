import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	text: {
		marginTop: 16,
		fontWeight: "bold",
		position: "absolute",
		left: 0,
	},
	subtitle: {
		marginTop: 16,
		fontWeight: "bold",
		position: "absolute",
		left: "28%",
	},
	bold: {
		fontWeight: "bold",
	},
});

export default styles;
